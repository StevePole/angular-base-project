!function(angular) {
    angular.module("my-application.controllers", []), angular.module("my-application.services", []), 
    angular.module("my-application.directives", []), angular.module("my-application.filters", []), 
    angular.module("my-application.mocks", []), angular.module("my-application", [ "ngRoute", "ngAnimate", "my-application.controllers", "my-application.directives", "my-application.filters", "my-application.services" ]);
}(window.angular), function(angular) {
    "use strict";
    function config($routeProvider) {
        $routeProvider.when("/my-resources", {
            templateUrl: "templates/my-resources.html",
            controller: "MyResourcesController",
            controllerAs: "vm"
        }).when("/my-resources/:id", {
            templateUrl: "templates/my-resource.html",
            controller: "MyResourceController",
            controllerAs: "vm"
        }).otherwise({
            redirectTo: "/my-resources"
        });
    }
    angular.module("my-application").config(config);
}(window.angular), function(angular) {
    testApp = angular.module("my-application.E2E", [ "my-application", "ngMockE2E" ]), 
    testApp.run(function($httpBackend) {
        var myResources = [ {
            id: 1,
            name: "Test"
        }, {
            id: 2,
            name: "Test 2"
        } ], paths = {
            myResources: {
                get: "json/my-resources.json"
            }
        };
        $httpBackend.whenGET(paths.myResources.get).respond(myResources), $httpBackend.whenGET(/^/).passThrough();
    });
}(window.angular);